{% extends 'base.html.twig' %}
{% block title %}Materiel{% endblock %}
{% block content %}

    <style>
    .example-wrapper { margin: 1em auto; max-width: 15  00px; width: 880px; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h1>Materiel</h1>
    <table class="table table-striped table-hover table-bordered border-info">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ materiel.name }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ materiel.quantity }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('materiel/_delete_form.html.twig') }}
    <a <button class="btn btn-secondary" href="{{ path('app_materiel_index') }}">retour à la liste</a> </button>
    <a <button class="btn btn-primary"  href="{{ path('app_materiel_edit', {'id': materiel.id}) }}">Éditer</a> </button>
</div>
{% endblock %}
