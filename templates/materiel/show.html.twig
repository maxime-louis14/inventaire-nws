{% extends 'base.html.twig' %}

{% block title %}Materiel{% endblock %}

{% block body %}
    <h1>Materiel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materiel.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ materiel.name }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ materiel.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_materiel_index') }}">retour à la liste</a>

    <a href="{{ path('app_materiel_edit', {'id': materiel.id}) }}">Éditer</a>

    {{ include('materiel/_delete_form.html.twig') }}
{% endblock %}
